{
  "name": "@hazeen/stylelint-config-scss",
  "version": "0.0.0",
  "description": "Scss stylelint config for hazeen",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "eslint": "eslint . --ext '.js'",
    "lint": "npm run eslint -- --cache --cache-location './dist/.eslintcache'",
    "lint:fix": "npm run eslint -- --fix"
  },
  "dependencies": {
    "stylelint-config-sass-guidelines": "9.0.1",
    "stylelint-scss": "4.3.0"
  },
  "devDependencies": {
    "@hazeen/eslint-config-base": "*",
    "@hazeen/eslint-config-base-prettier": "*"
  }
}
